INCLUDE_DIRECTORIES(SYSTEM
	${PROJECT_SOURCE_DIR}/src
)

SET(PROJECT_SOURCE
	func.c
)

ADD_LIBRARY(project ${PROJECT_SOURCE})

SET(PROJECT_TEST_SOURCE main.c)

ADD_EXECUTABLE(project_test ${PROJECT_TEST_SOURCE})

TARGET_LINK_LIBRARIES(project_test project)